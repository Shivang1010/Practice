1. Write an sql query to fetch firstname from worker table using alias name as <workeralli>
Ans: Select Firrstname as workeralli from worker;

2. write sql query to fetch worker name from worker table in upper case
ans: Select upper(firstname) from worker;

3. fetch unique value of department from worker tble 
Ans: Select distinct Department from worker;

4.print the first three charachter of first name from worker table
Ans: Select substring(Firstname,1,3) from worker;

5. sql query to find position of the alphabet a in the first name column 'amitabh' from worker table
Ans: Select INSTR(firstname, Binary'a') from Worker where Firstname='Amitabh';
note: INSTR method is in case sensitive by default, using binary operator operator will make instr work as the case sensitive functin 

6. sql query to print first name from worker table after removing the white spaces from the right side
Ans: Select RTRIM(Firstname) from worker;

7. Sql query to print the department from worker table after removing spaces from the left side
Ans: Select LTRIM(Department) from worker;

8. sql query that fetches the unique values of department fro worker table and prints its length
Ans: Select distinct length(Department) from worker;

9.sql query to print first name from worker table after replacing a with A
ans: Select REPLACE(Firstname, 'a','A') from worker;

10.sql query to print first name and last name from worker table into a single column completenae. A space char should sepARATE THEM
aNS Select CONCAT(firstname,' ',lastname) AS 'COMPLETENAME' from worker;

11. sql query to print all worker details from the worker table order by firstname in ascending order
Ans: Select* from WOrker order bby firstname asc;

12. sql query to print all worker details from the worker tabe order by first name in ascending order and department in descending
Ans: Select* from WOrker order by firstname asc, Department desc

13. Sql query to print details for worker with the first name as vipul and satish from worker table 
Ans: Select* from worker where Firstname in ('vipul','satish');

14. sql query to print details of worker excluding first name vipul and satish from worker table
Ans: Select * from WOrker where firstname not in ('vipul', 'satish');

15. Sql query to print details of worker with department name as 'admin"
Ans. Select * from worker where departmentname like 'Admin%';

16. sql query to print details of the worker whose firstname contain 'a'
Ans. Select* from worker where firstname like '%a%';

17. sql query to print details of the worker whose fissrt name ends with a
and. Select * from worker where firstname like '%a';

18. sql query to print details of the worker whose first name ends with 'h' and contains six alphabets
Ans. Select * from worker where firstname like '_____h';

19. sql query to print details of the worker whose salary lies between 10000 and 50000
Ans. Select * from Worker where Salary between 10000 and 50000;

20. sql query to print details of the worker who have joined in feb 2014
Ans. Seect * from Worker where year(Joining) =2014 and month(Joining) = 2;

21. Sql query to fetch the count of employee working in the department 'admin'
Ans. Select Count(*) From wrker WhERE DEPARTMENT = 'admin';

22. Sql query to fetch worker name with salaries>=50000 and <=10000
Ans. Select CONCAT(Firstname,' ',Lastname) As Workername, Salary 
From WOrker
WHere Workerid in
(Select Workerid from Worker where Salary between 50000 and 100000);

23. sql query to fetch no.of worker for each department in descending order
Ans. SELECT DePARTMENT, count(WORKERID) noofworker
from worker 
group by department 
order by noofworker desc;

24.Sql quer to print details of the worker who are also manager
Ans. Select DISTINCT W.Firstname, T.WOrertitle
from worker W
Inner join title T
On W.workerid=t.workerrefid
and t.workertitle in ('Manager');

25. sql query to fetch duplicate records having matching data in some fields of table
Ans. Select workertitle,affectedfrom, Count(*)
From title
group by workertitle, affectedfrom
having ount(*) >1;

26. sql query to show only odd rows from a table
An. Select * from worker mod(WOrkerid,2)<>0;

27. SQl query to show only even row
Ans. Select * from worker mod(workerid,2)=0;

28. sql query to clone a new tabe from another table
Ans. Select * into workerclone from worker; // the general query to clone a table with data
Select * into workerclone from worker where 1=0; // the general way to clone a tbale without information

29. sql query to fetch interesting records of two tables
Ans. (Select * from WOrker) Intersect (Select * From workerclone);

30. Sql query to show records from one table that another table does not have
Ans. Select * from worker minus Select * from titles;

31. Sql query to show the current date and time
ANs. SELECT CURDATE();//date
Select now();//datetime

32. sql query to show top n(say 10) records of a table
Ans. Select * from worker order by salary DESC Limit 10;

33.SQL query to determine the nth(say n=5) highest salary from a table
Ans. Select Salary from Worker Order By Salar DESC LIMIT n-1,1;

34. Write an SQL query to determine the 5th highest salary without using TOP or limit method.
Ans.SELECT Salary
FROM Worker W1
WHERE 4 = (
 SELECT COUNT( DISTINCT ( W2.Salary ) )
 FROM Worker W2
 WHERE W2.Salary >= W1.Salary
 );
 
 35. Second highest salary
 ANs. Select max(Salary) from Worker 
 where Salary not in (Select max(Salary) from Worker);
 
 36.
